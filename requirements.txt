espn-api==0.45.1
pandas==2.3.1

# boto3 is deliberately not pinned/listed here: the Lambda execution
# environment provides it (and botocore) preinstalled, and who_dat/config.py
# / lambda_function.py only import it lazily/at Lambda-entrypoint scope, so
# local-only dev (scripts/run_all.py etc.) never needs it. `pip install
# boto3` locally only if exercising the S3/Secrets Manager config backend
# (WHO_DAT_CONFIG_BACKEND=s3) outside of Lambda.
